import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    actions: {
        flexDirection: "row",
        alignItems: "center",
        marginTop: 8,
    },
    button: {
        padding: 4,
    },
    quantity: {
        fontSize: 16,
        fontWeight: "bold",
        marginHorizontal: 12,
    },
});
